eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka

spring:
  application:
    name: order
  datasource:
    url: jdbc:mysql://localhost:3306/restaurant_order?createDatabaseIfNotExist=true
    username: root
    password: 2025
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 8082